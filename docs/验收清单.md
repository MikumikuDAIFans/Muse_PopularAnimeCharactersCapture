# Muse DataLoad 验收清单

Last updated: 2026-04-25 18:10 Asia/Shanghai

状态说明：

| 状态 | 含义 |
|---|---|
| 通过 | 当前代码、数据与运行环境已经验证通过 |
| 后续增强 | 不阻塞本轮交付，但建议后续继续硬化 |

## A. 综合热门角色榜

| ID | 要求 | 实现/产物 | 验证方式 | 状态 | 结论 |
|---|---|---|---|---|---|
| A-001 | 仅使用 Danbooru 内部数据 | `backend/services/danbooru.py`, `backend/services/ingest.py` | 代码审查、同步脚本与 DB 来源核对 | 通过 | 无外部榜单或人工热度源 |
| A-002 | 仅使用 character tag 作为角色候选 | `Tag.category == "character"`, `backend/services/ranking.py` | 榜单校验与 tag 聚合逻辑 | 通过 | 非 character tag 不进入候选池 |
| A-003 | 输出最近半年综合热门角色 Top 200 | `output/exports/character_list_recent_6m_top_200.json` | JSON 计数 200 | 通过 | 正式 JSON 已生成 |
| A-004 | 同步输出 CSV | `output/exports/character_list_recent_6m_top_200.csv` | 文件存在且校验通过 | 通过 | CSV 作为人工查看格式 |
| A-005 | JSON/CSV 一致性 | `scripts/validate_character_export.py` | `--expect-count 200` | 通过 | `OK character export rows=200` |
| A-006 | 字段包含 rank、角色、作品、帖数、近期帖数、热度分 | `backend/services/ranking.py` | 校验脚本 | 通过 | 字段满足后续抓取与人工查看 |
| A-007 | copyright 覆盖率可审计 | `scripts/audit_copyright_coverage.py`, `output/reports/top200_review.csv` | 覆盖率审计、自动抽检表 | 通过 | Top 200 自动抽检 200/200 pass |
| A-008 | 结果可驱动样本导出 | `/api/datasets/export` | 随机多角色真实导出 | 通过 | 多角色导出回归通过 |

## B. 新兴热门角色榜

| ID | 要求 | 实现/产物 | 验证方式 | 状态 | 结论 |
|---|---|---|---|---|---|
| B-001 | 补齐首次出现时间 | `first_seen_at`, `scripts/enrich_character_birthdates.py` | DB 字段与导出字段核对 | 通过 | 首次出现时间已缓存 |
| B-002 | 记录首次出现帖子 | `first_seen_post_id` | 导出字段核对 | 通过 | 已写入新兴榜 |
| B-003 | 年龄按当前时间计算 | `backend/services/emerging.py`, `frontend/src/pages/DataPage.vue` | 缓存读取与前端展示核对 | 通过 | 使用“当前时间 - 首次出现” |
| B-004 | 近期占比 `recent_ratio` | `backend/services/emerging.py` | `validate_emerging_export.py` | 通过 | 范围校验 0 到 1 |
| B-005 | 新兴热度 `growth_score` | `backend/services/emerging.py` | 排序字段核对 | 通过 | 新兴榜独立按 growth score 排序 |
| B-006 | 年龄阈值过滤老角色 | `max_age_days=1095` | 导出校验与抽查 | 通过 | 古早角色不会因历史总量进入新兴榜 |
| B-007 | 独立 JSON 输出 | `output/exports/character_list_emerging_6m_top_200.json` | JSON 计数 128 | 通过 | 当前真实数据产出 128 条 |
| B-008 | 独立 CSV 输出 | `output/exports/character_list_emerging_6m_top_200.csv` | CSV 校验 | 通过 | 与 JSON 数量一致 |
| B-009 | 新兴榜 API | `GET /api/characters/emerging` | backend/frontend 代理均返回 128 | 通过 | API 可用 |
| B-010 | 新兴榜前端 | `frontend/src/pages/DataPage.vue` | 代码审查、构建通过 | 通过 | 只保留“刷新”，按行导出样本 |

## C. 训练样本导出

| ID | 要求 | 实现/产物 | 验证方式 | 状态 | 结论 |
|---|---|---|---|---|---|
| C-001 | 按角色导出样本目录 | `backend/services/dataset.py` | `/api/datasets/export` | 通过 | 目录在 `output/dataset/<character>` |
| C-002 | 每条样本包含图片 | `_download_file` fallback URL | `validate_dataset.py` | 通过 | 随机导出角色均有真实图片 |
| C-003 | 每条样本包含 txt caption | `build_caption` | `validate_dataset.py` | 通过 | txt 与 json `caption_v1` 一致 |
| C-004 | 每条样本包含 json 元数据 | `dataset.py` | `validate_dataset.py` | 通过 | 包含 post、tag groups、caption、fav_count 等字段 |
| C-005 | Caption 顺序稳定 | subject -> character -> copyright -> artist -> general -> other | 单测与代码审查 | 通过 | 顺序满足 v0.1 |
| C-006 | 下载失败有记录 | `DownloadLog`, API `errors` | 代码审查、导出回归 | 通过 | 成功/跳过/失败均入库或返回 |
| C-007 | 原图 403 降级下载 | `file_url`, `sample_url`, `preview_url`，必要时 live post 补查 | silver_wolf 回归 | 通过 | `silver_wolf_(lv.999)...` 有 5 组 jpg/json/txt |
| C-008 | 综合榜和新兴榜按行样本导出 | 前端统一弹窗 | 前端代码审查、构建 | 通过 | 两个榜单共用导出弹窗 |

## D. 服务化、前端与工具链

| ID | 要求 | 实现/产物 | 验证方式 | 状态 | 结论 |
|---|---|---|---|---|---|
| D-001 | Docker 后端健康 | `docker-compose.yml` | `docker-compose ps` | 通过 | backend `Up ... (healthy)` |
| D-002 | 前端服务运行 | nginx frontend | `docker-compose ps` | 通过 | frontend `Up`，端口 8080 |
| D-003 | 前端 `/api` 代理 | `nginx.conf` | `http://127.0.0.1:8080/api/stats` | 通过 | 返回 26,400 posts |
| D-004 | 后端 API smoke | `scripts/smoke_api.py` | `--root http://localhost:8001` | 通过 | `OK api smoke` |
| D-005 | SQLite 持久化 | Docker volume `./muse_dataload.db:/app/muse_dataload.db` | `/api/stats` | 通过 | 26,400 posts 可查 |
| D-006 | Python 编译检查 | backend/cli/scripts/tests | `python -m compileall` | 通过 | 无编译错误 |
| D-007 | 单元测试 | `tests/` | `pytest -q` | 通过 | 5 passed |
| D-008 | 前端生产构建 | `frontend/` | `npm run build` | 通过 | Vite build success |
| D-009 | 前端数据页烟测 | Playwright 打开 `/data` | 页面文本与按钮核对 | 通过 | 新兴榜加载 128 条，无“重建”按钮，128 个按行导出按钮 |

## E. 收尾回归

| ID | 要求 | 验证方式 | 状态 | 结论 |
|---|---|---|---|---|
| E-001 | 综合榜导出文件数量准确 | JSON 计数 | 通过 | 200 条 |
| E-002 | 新兴榜导出文件数量准确 | JSON 计数 | 通过 | 128 条 |
| E-003 | backend 与 frontend API 数量一致 | `/api/characters/top`, `/api/characters/emerging` | 通过 | backend/frontend 均返回 200/128 |
| E-004 | 随机多角色批量导出 | 5 个随机角色，每个 2 条 | 通过 | 共 10 条样本，图片/txt/json 均存在 |
| E-005 | 银狼图片导出修复保持有效 | `validate_dataset.py output/dataset/silver_wolf_(lv.999)_(honkai__star_rail)` | 通过 | 5 组 jpg/json/txt |

## F. 近两年全站元数据执行准备

| ID | 要求 | 实现/产物 | 验证方式 | 状态 | 结论 |
|---|---|---|---|---|---|
| F-001 | 全站阶段只抓元数据，不下载图片 | `scripts/sync_recent_posts.py --skip-import` | 代码审查、help 命令 | 通过 | 默认 Worker 只写 JSONL，未触发图片下载 |
| F-002 | 支持近两年窗口 | `--recent-months 24` | 脚本参数 | 通过 | 可按当前日期回退 24*30 天，也可用 `--start-date/--end-date` 固定窗口 |
| F-003 | 支持分片长跑 | `--shard monthly|quarterly` | 单元测试 | 通过 | monthly 会拆成自然月片段 |
| F-004 | 支持断点续跑 | `PostCrawlerWorker.resume`, checkpoint `last_cursor` | 单元测试 | 通过 | 重跑同一 task id 会从 checkpoint cursor 继续 |
| F-005 | 每片有运行统计 | `output/metadata/sync_recent_posts_manifest.json` | 脚本输出与 manifest 写入 | 通过 | 记录行数、unique ids、duplicate ids、错误数 |
| F-006 | JSONL 可先流式聚合候选榜 | `scripts/build_character_candidates_from_jsonl.py` | 单元测试、help 命令 | 通过 | 不依赖 SQLite，可输出候选 JSON/CSV |
| F-007 | 新笔记本迁移指导 | `docs/迁移与执行指南.md` | 文档审查 | 通过 | 包含 clone、环境、长跑、聚合、定向样本导出 |

## 后续增强项

| ID | 项目 | 状态 | 说明 |
|---|---|---|---|
| G-001 | 前端实时 WebSocket 日志消费 | 后续增强 | 不阻塞榜单、样本导出与 API 验收 |
| G-002 | 百万级 SQLite 查询压测 | 后续增强 | 近两年全站 JSONL 优先流式聚合；完整入库前仍需压测 |
| G-003 | 容器停止中的任务优雅退出 | 后续增强 | 当前已有文件级断点和显式 `resume` 参数，仍可硬化 |
| G-004 | 新兴榜前 20 名业务复核 | 后续增强 | Danbooru 早期误标或 alias 历史变更可能影响出生日期 |
| G-005 | 两年窗口真实规模评估 | 后续增强 | 新笔记本长跑前先用 `--limit` 灰度测算吞吐与磁盘 |

## 总体判断

综合榜、新兴榜、样本导出、Docker 服务化、前端构建、API smoke 与随机多角色真实导出回归均已通过。近两年全站元数据 JSONL 长跑所需的分片、resume、manifest、流式候选榜和迁移指导也已补齐。当前项目达到迁移后可继续执行的状态。
